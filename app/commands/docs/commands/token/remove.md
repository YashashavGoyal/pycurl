### token remove
Remove a token or all tokens.

**Options:**
- `--alias ALIAS`: Remove specific token alias.
- `--all`: Remove all tokens.

**Example:**
```bash
pycurl token remove --alias my-token
pycurl token remove --all
```
