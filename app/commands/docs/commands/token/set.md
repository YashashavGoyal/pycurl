### token set
Manually save a token to the token file.

**Options:**
- `--alias ALIAS`: Name for the token.
- `--token TOKEN`: The actual token string.

**Example:**
```bash
pycurl token set --alias my-token --token "EY..."
```
