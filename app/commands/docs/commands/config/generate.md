### config generate
Interactive tool to generate or update the configuration. [Recommended]

**Options:**
- `--interactive`: Enable interactive mode.
- `--reset`: Reset to defaults.

**Example:**
```bash
pycurl config generate --interactive
```
